# Core dependencies
numpy>=1.24.0
pyaudio>=0.2.11
soundfile>=0.12

# Torch is used only for Silero VAD, so the CPU build is the right one: it
# keeps the venv ~1.5GB instead of ~5.2GB and removes the CUDA dependency
# that broke this app while the dGPU was passed through to the Windows VM.
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchaudio>=2.0.0
silero-vad>=4.0.1

# STT
faster-whisper>=0.9.0

# TTS: kokoro-onnx is the default engine. onnxruntime (NOT -gpu) keeps
# synthesis on the CPU so speech survives GPU passthrough.
kokoro-onnx>=0.4.0
onnxruntime>=1.16.0

# LLM
anthropic>=0.8.0
openai>=1.0.0

# Optional alternative TTS engines, selected with VOICE_ASSISTANT_TTS_ENGINE:
#   pocket-tts     # kyutai Pocket TTS  (~100M, CPU-realtime, preset voices)
#   supertonic     # Supertonic 3       (~99M, ONNX, 31 languages)
